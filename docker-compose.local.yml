services:
  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    container_name: jtrack-nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx/dev/conf.d/default.conf:/etc/nginx/conf.d/default.conf
